# Основные зависимости
numpy>=1.20.0
openai>=1.0.0
pydub>=0.25.1

# Для обработки аудио
scipy>=1.8.0
sounddevice>=0.4.6
ffmpeg-python>=0.2.0

# Дополнительные библиотеки для захвата аудио
pyaudio>=0.2.11
comtypes>=1.1.10  # Для Windows WASAPI
pycaw>=20220416  # Для управления Windows аудио

# Для распознавания речи
faster-whisper>=1.0.0  # 2-4x быстрее openai-whisper, не требует torch

# VAD (Voice Activity Detection)
# faster-whisper включает встроенный VAD на основе Silero.

# Для пользовательского интерфейса
# tkinter обычно уже установлен с Python

# Дополнительные утилиты
python-dotenv>=1.0.0
requests>=2.28.0  # Для HTTP запросов
pillow>=9.0.0  # Для обработки изображений (если понадобится для UI)